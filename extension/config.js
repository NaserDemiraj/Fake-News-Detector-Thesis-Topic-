// Backend base URL. Change this to your deployed API when publishing the extension,
// and add the matching host to "host_permissions" in manifest.json.
const BACKEND_URL = 'http://localhost:5000';
const APP_URL = 'http://localhost:3000';
