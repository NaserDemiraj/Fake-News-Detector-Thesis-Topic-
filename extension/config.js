// Backend + app URLs. Point at the live deployment. When running locally, swap these
// for http://localhost:5000 / http://localhost:3000 and add the matching hosts to
// "host_permissions" in manifest.json.
const BACKEND_URL = 'https://naserd-fake-news-backend.hf.space';
const APP_URL = 'https://fake-news-detector-thesis-topic.vercel.app';
